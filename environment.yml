name: Yads
channels:
  - pytorch-nightly
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib

  - black
  - pytest
  - coverage 

  - pip:
      - pydoe==0.3.8
      - pyevtk==1.6.0
      
prefix: /opt/anaconda3/envs/Yads
